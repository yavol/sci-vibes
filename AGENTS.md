## Local Guidance

- Use `scripts/dev-server.sh start|stop|restart|logs [port]` for local development.
  It runs Vite in a checkout-specific tmux session and writes its output to a
  matching log file under `/tmp`.
